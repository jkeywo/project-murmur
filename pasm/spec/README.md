# Project Murmur PASM specification

This directory is the source of truth for Project Murmur's planned system
boundaries and gameplay design. The entries presently record architecture only;
they do not map to an implementation or add gameplay code.

Start implementation only after the first vertical slice has a matching PASM
entity and an implementation mapping.
